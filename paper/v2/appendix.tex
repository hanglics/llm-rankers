\section{Prompt Templates}
\label{sec:prompts}

\textit{[Final prompt templates for MaxContext-DualEnd, MaxContext-TopDown,
and MaxContext-BottomUp under the numeric label scheme $1 \dots N$ are
included here once the camera-ready release is prepared.]}

\section{Hyperparameters and Implementation Notes}
\label{sec:hparams}

\textit{[Reserved. Will include: full $4 \times 6 \times 3$ raw nDCG@10
table with paired-bootstrap CIs and per-cell H1 verdicts; Bonferroni and
Holm-Bonferroni adjusted $p$-value columns; Tier-2 size sweep
(Qwen3.5-\{0.8B, 2B, 27B\} + Ministral 3-\{3B, 14B\}); per-BEIR-domain
breakdown; per-cell input-order-stability statistics over the 10
seeds; order-robustness pilot (BM25-forward / inverse / random) at
$N{=}50$; passage-length sweep at $\ell \in \{64, 128, 256, 512\}$; the
original-Setwise vs MaxContext-TopDown direct-baseline smoke; SLURM
launchers and run-manifest with HF revision hashes for every job.]}
